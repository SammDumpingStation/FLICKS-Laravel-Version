@props(['label', 'title'])

<div class="flex items-end">
    <label class="flex-1 basis-96 text-2xl text-grey">{{ $label }}</label>
    <h3 class="flex-1 basis-full text-3xl font-semibold">{{ $title }}</h3>
</div>
