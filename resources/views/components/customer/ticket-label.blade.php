@props(['label', 'title'])

<div class="flex items-center">
    <label class="flex-1 basis-96 text-xl text-grey">{{ $label }}</label>
    <h3 class="flex-1 basis-full text-3xl font-semibold">{{ $title }}</h3>
</div>
